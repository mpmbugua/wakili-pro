# Railway Backend Environment Variables
# Add these to your Railway backend service environment

# Frontend CORS Configuration
FRONTEND_URL=https://wakili-pro-frontend.vercel.app
CORS_ORIGIN=https://wakili-pro-frontend.vercel.app

# Additional allowed origins (comma-separated)
ALLOWED_ORIGINS=https://wakili-pro-frontend.vercel.app,https://wakili-pro.vercel.app,https://wakili-pro.netlify.app

# Database (automatically injected by Railway)
# DATABASE_URL=postgresql://...

# JWT Configuration  
JWT_SECRET=${JWT_SECRET}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}

# OpenAI Integration
OPENAI_API_KEY=${OPENAI_API_KEY}

# Node Environment
NODE_ENV=production
import { Response } from 'express';
import { prisma } from '../utils/database';
import { AuthenticatedRequest } from '../middleware/auth';

// POST /analytics/event - log a feature event
// logFeatureEvent removed: featureEvent model does not exist in schema.

// POST /analytics/event - log a feature event
// logFeatureEvent removed: featureEvent model does not exist in schema.

// GET /analytics/feature-events - aggregate feature event stats
// getFeatureEventStats removed: featureEvent model does not exist in schema.

// GET /analytics/feature-events - aggregate feature event stats
// getFeatureEventStats removed: featureEvent model does not exist in schema.
